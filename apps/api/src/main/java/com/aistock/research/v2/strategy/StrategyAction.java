package com.aistock.research.v2.strategy;

public enum StrategyAction {
    ADD,
    LIGHT_TRIAL,
    HOLD,
    NEXT_WATCH,
    WAIT_PULLBACK,
    WAIT,
    REDUCE,
    EXIT,
    DATA_BLOCKED,
    RISK_BLOCKED
}
